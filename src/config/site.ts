/**
 * Constantes de l'application.
 *
 * Le nom est provisoire : le client ne l'a pas encore arrêté. Il n'existe qu'ici,
 * et tout le reste le lit — écran de connexion, barre latérale, titre d'onglet.
 * Le jour où il tranche, une seule ligne change.
 */
export const siteConfig = {
  nom: 'Portail',
  description: 'Application interne de gestion',
} as const
